<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
    <link href="{{constant('ECweb\\Bootstrap::ENTRY_URL')}}css/style.css" rel="stylesheet">
</head>

<body>
<header>
  {% include 'header_login_top.html.twig' %}
</header>

<div class = "page_body">
  <div id="wrapper">
    <div>
      <p>購入履歴</p>
      <a href="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}mypage.php">マイページへ戻る</a>
    </div>

  <div = "purcase_list">
    {% for value in dataArr %}
    <div class="item">
      <ul>
        <li class="image"><img class = "detail_image" src="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}images/{{value.image}}" alt="{{value.item_name}}"></li>
        <li class="name">{{value.item_name}}</li>
        <li class="price">購入日時：{{value.regist_date}}</li>
        <li class="price">購入ユニット：{{value.purchase_unit}}</li>
        <li class="price">&yen;{{value.price|number_format(0, '.', ',')}} × {{value.quantity}}個　　小計 &yen;{{value.price * value.quantity|number_format(0, '.', ',')}}</li>
        <li class="price"><a href="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}detail_item.php?item_id={{value.item_id}}" >詳細</a></li>
      </ul>
    </div>
    {% endfor %}
  </div>
  </form>
</div>
</div>

<div>
  {% include 'footer.html.twig' %}
<div>

</body>
</html>