<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="{{constant('ECweb\\Bootstrap::ENTRY_URL')}}css/style.css" rel="stylesheet">
  </head>

  <body>
    <header>
      {% include 'header_login.html.twig' %}
    </header>

    <div class = "page_body">
      <div id="cart_list">
        <p>その商品は既にショッピングカートに入っています。</p>
        <p><a href="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}cart.php">カートへ（数量変更）</a></p>
      </div>
    </div>

    <div>
      {% include 'footer.html.twig' %}
    </div>
  </body>
</html>