<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title></title>
  <link href="{{constant('ECweb\\Bootstrap::ENTRY_URL')}}css/style.css" rel="stylesheet">
</head>

<body>
    <div id = "page_body">
    {{照会したい日付を選択してください。}}
      <form method="post" action="">
        <div id="time_from">
          <input type ="datetime-local" name="time_from" value="{{timeArr.time_from}}" required>
        </div>
        <div id="time_to">
          <input type ="datetime-local" name="time_to" value="{{timeArr.time_to}}" required>
        </div>
        <input type="submit" name="search" value="検索">
      </form>

    <div>
    {% if timeArr is not null %}
    検索条件: {{timeArr.from}} {{timeArr.mark}} {{timeArr.to}}
    {% endif %}    
    </div>

    <table border="1">
    <tr>
      <th>購入ID</th>
      <th>購入ユニット</th>
      <th>顧客No.</th>
      <th>商品ID</th>
      <th>商品名</th>
      <th>価格</th>
      <th>画像</th>
      <th>数量</th>
      <th>登録日</th>
    </tr>

  {% for value in salesArr %}
  <tr>
    <td>
      {{value.purchased_id}}
    </td>

    <td>
    {{value.purchase_unit}}
    </td>

    <td>
    {{value.customer_no}}
    </td>

    <td>
    {{value.item_id}}
    </td>

    <td>
    {{value.item_name}}
    </td>

    <td>
    {{value.price}}
    </td>

    <td>
    {{value.image}}
    </td>

    <td>
    {{value.quantity}}
    </td>

    <td>
    {{value.regist_date}}
    </td>
  </tr>
  {% endfor %}
</table>

  {# <div class="sales_search_button">
  <input type="submit" name="csv_output" value="CSV出力">
  </div> #}

  <div><a href="{{constant('ECweb\\Bootstrap::ENTRY_URL_ADMIN')}}admin_top.php">管理TOPページへ</a></div>

</body>
</html>