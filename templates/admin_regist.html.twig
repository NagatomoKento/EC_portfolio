<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8">

    <link href="{{constant('ECweb\\Bootstrap::ENTRY_URL')}}css/style.css" rel="stylesheet">

    <title>商品登録</title>
  </head>

  <body>
  <div>
    <form method="post" action="admin_confirm.php" enctype="multipart/form-data">

        <div id="item_name">
          <label for="item_name">商品名</label><br>
          <input type ="text" name="item_name" value="{{itemArr.item_name}}" required>
        </div>

        <div id="detail">
          <label for="detail">商品詳細</label><br>
          <textarea  name="detail" value="{{itemArr.detail}}" cols="50" rows="5" required></textarea>
        </div>

        <div id="price">
          <label for="price">価格</label><br>
          <input type="text" name="price" value="{{itemArr.price}}" id="price" size="8" maxlength="8" required>円
        </div>


        <div id="image">
          <div id="image">
            <label for="image">画像</label><br>
            <input type="file" name="image" accept="image/jpeg, image/jpg, image/png" onchange="previewImage(this);">

            <p>Preview:<br>
            <img id = "preview1" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEA0w==">
            </p>
            <script>
            function previewImage(obj)
            {
              var fileReader = new FileReader();
              fileReader.onload = (function(){
                document.getElementById('preview1').src = fileReader.result;
              });
              fileReader.readAsDataURL(obj.files[0]);
            }
           </script>
          </div>
        </div>

        <div id="ctg_id">
          <label for="news_admin">カテゴリー</label><br>
          <select name = "ctg_id">
            <option value = "1">野菜</option>
            <option value = "2">果物</option>
            <option value = "3">牛肉</option>
            <option value = "4">鳥肉</option>
            <option value = "5">お米</option>
          </select>
        </div>

        <div>
          <input type="submit" name="confirm" value="アップロード確認へ">
        </div>
      </form>
</div>

  </body>
</html>