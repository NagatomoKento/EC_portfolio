<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
      <link href="{{constant('ECweb\\Bootstrap::ENTRY_URL')}}css/style.css" rel="stylesheet">
  </head>

  <body>
    <header>
      {% include 'header_login_top.html.twig' %}
    </header>

    <div class = "page_body">
      <div id="cart_list">
        <p>ご購入内容確認</p>
        <a href="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}cart.php">カートへ戻る</a>
        <p>カート内商品数：{{sumNum}}個</p>
      </div>

      {% for value in dataArr %}
      <div class="item">
        <ul>
          <li class="image"><img class = "detail_image" src="{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}images/{{value.image}}" alt="{{value.item_name}}"></li>

          <li class="name">{{value.item_name}}</li>

          <li class="price">&yen;{{value.price|number_format(0, '.', ',')}} × {{value.NUM}}個</li>
          <li class="price">小計 &yen;{{value.price * value.NUM|number_format(0, '.', ',')}}</li>
        </ul>
      </div>
      {% endfor %}

      <div>
      購入しますか？
        <form method="post" action = "{{constant('ECweb\\Bootstrap::ENTRY_URL_SHOP')}}shop_complete.php">
        
        <input type="submit" value="購入する">
        </form>
      </div>
    </div>

    <div>
      {% include 'footer.html.twig' %}
    </div>
  </body>
</html>